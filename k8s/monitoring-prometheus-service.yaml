apiVersion: v1
kind: Service
metadata:
  name: travelforge-prometheus
  namespace: travelforge
  labels:
    app.kubernetes.io/name: travelforge-prometheus
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: travelforge-prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
