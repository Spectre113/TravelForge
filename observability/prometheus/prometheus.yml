global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: travelforge-backend
    metrics_path: /metrics/prometheus
    static_configs:
      - targets:
          - host.docker.internal:5000

  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090
